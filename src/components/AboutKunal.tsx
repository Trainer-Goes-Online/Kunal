/* DEPRECATED — the LP's founder beat is now Beat 5 `Founder` (SDP dark band:
   credential pills + narrative text + cert). Kept as a re-export so any stale
   import still resolves. The old dark-graphite AboutKunal is retired here. */
export { Founder as AboutKunal } from "./Founder";
