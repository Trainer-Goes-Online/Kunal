/* DEPRECATED — the LP's offer beat is now Beat 8 `Programme` (SDP seven-item
   ledger, brass-on-light). Kept as a re-export so any stale import still resolves.
   The old dark-graphite Offer (OfferLedger + four-costs + inline guarantee) is
   retired for this funnel. */
export { Programme as Offer } from "./Programme";
